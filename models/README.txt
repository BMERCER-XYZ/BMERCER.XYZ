Place your ASCII or binary STL files here.

Usage in URL:
- Default: loads models/model.stl if present.
- Override: add ?stl=your_file.stl to the page URL.

Tips:
- Keep file sizes modest (<5 MB) for performance.
- ASCII STL recommended for easier inspection.
